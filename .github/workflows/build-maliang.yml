name: Build Perfect Maliang Image (Full UI)

on:
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. 拉取源码
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. 设置 Java 环境 (后端需要)
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3. 设置 Flutter 环境
      # 保持使用 3.24.0 以解决 flutter_syntax_view 的问题
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'

      # 4. 编译前端 Web (修复依赖冲突的关键步骤)
      - name: Build Frontend (Flutter Web)
        run: |
          echo "Starting Flutter Web Build..."
          cd AINoval
          
          # 👇【关键修复】强制指定 intl 版本，解决 dependencies conflict
          echo "Fixing intl dependency conflict..."
          flutter pub add intl:^0.19.0
          
          # 清理缓存并获取依赖
          flutter clean
          flutter pub get
          
          # 编译 Web 版本
          # 使用 auto 渲染器：电脑用 CanvasKit (高性能)，手机用 HTML (加载快，兼容iOS)
          flutter build web --release --web-renderer auto
          echo "Frontend build complete."

      # 5. 组装文件结构 (匹配 Dockerfile 要求)
      - name: Prepare Build Directory
        run: |
          # 创建目标目录
          mkdir -p deploy/dist
          
          # A. 移动编译好的前端文件到 web 目录
          mv AINoval/build/web deploy/dist/web
          
          # 复制一份 admin_web 以防万一 (兼容不同 Dockerfile 写法)
          cp -r deploy/dist/web deploy/dist/admin_web
          
          # B. 下载后端 JAR 包
          echo "Downloading Backend JAR..."
          # 下载 v1.0.0 正式版 JAR
          wget -O deploy/dist/ainovel-server.jar https://github.com/Deng-m1/MaliangAINovalWriter/releases/download/v1.0.0/ainovel.jar
          
          # C. 打印目录结构验证
          echo "Final Build Context Structure:"
          ls -R deploy/dist/

      # 6. 登录 Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 7. 设置构建工具
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # 8. 构建并推送最终镜像
      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: deploy/open/Dockerfile
          push: true
          # 强制指定架构为 linux/amd64 (确保群晖能跑)
          platforms: linux/amd64
          # 镜像标签
          tags: ${{ secrets.DOCKER_USERNAME }}/maliang-writer:latest
