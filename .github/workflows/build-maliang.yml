name: Build Perfect Maliang Image (Full UI)

on:
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. 拉取完整源码
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. 设置 Java 环境 (后端需要)
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3. 设置 Flutter 环境
      # 保持 3.19.6 以维持对主要代码的兼容性
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'

      # 4. 编译前端 Web (含多重依赖修复)
      - name: Build Frontend (Flutter Web)
        run: |
          echo "Starting Flutter Web Build..."
          cd AINoval
          
          # ---------------------------------------------------------
          # 👇【核心修复区域】强制降级冲突的依赖
          # ---------------------------------------------------------
          echo "Fixing dependency conflicts..."
          
          # 1. 修复 intl (SDK 限制)
          flutter pub add intl:^0.18.1
          
          # 2. 修复 flutter_lucide (Dart 版本限制)
          # 当前 Dart 3.3.4 无法运行 1.3.0+，降级到 0.5.0 以兼容
          flutter pub add flutter_lucide:^0.5.0
          
          # 清理并获取依赖
          flutter clean
          flutter pub get
          
          # 👇【代码生成】运行 build_runner
          # 这一步非常重要，用于生成 .g.dart 文件
          echo "Running build_runner..."
          flutter pub run build_runner build --delete-conflicting-outputs
          
          # 编译 Web 版本
          # 使用 html 渲染器以获得最佳兼容性，开启 verbose 查看详情
          flutter build web --release --web-renderer html --verbose
          
          echo "Frontend build complete."

      # 5. 组装文件结构
      - name: Prepare Build Directory
        run: |
          mkdir -p deploy/dist
          
          # 移动编译好的前端文件
          mv AINoval/build/web deploy/dist/web
          
          # 兼容性复制
          cp -r deploy/dist/web deploy/dist/admin_web
          
          # 下载后端 JAR
          echo "Downloading Backend JAR..."
          wget -O deploy/dist/ainovel-server.jar https://github.com/Deng-m1/MaliangAINovalWriter/releases/download/v1.0.0/ainovel.jar
          
          # 验证目录
          ls -R deploy/dist/

      # 6. 登录 Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 7. 设置构建工具
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # 8. 构建并推送
      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: deploy/open/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ secrets.DOCKER_USERNAME }}/maliang-writer:latest
