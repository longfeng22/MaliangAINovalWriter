name: Build Perfect Maliang Image (Full UI)

on:
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. 拉取完整源码
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. 设置 Java 环境 (后端需要)
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3. 设置 Flutter 环境 (关键调整)
      # 之前的 3.19.0 (Dart 3.3.0) 无法满足 >=3.3.3
      # 之前的 3.24.0 (Dart 3.5.x) 太新，导致代码编译失败
      # ✅ 修正为 3.19.6 (Dart 3.3.4)，这是最完美的兼容版本
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'

      # 4. 编译前端 Web (增加代码生成步骤)
      - name: Build Frontend (Flutter Web)
        run: |
          echo "Starting Flutter Web Build..."
          cd AINoval
          
          # 清理旧的依赖
          flutter clean
          flutter pub get
          
          # 👇【核心修复】运行代码生成器
          # 项目使用了 Hive，必须生成 .g.dart 适配器文件，否则编译必挂
          # --delete-conflicting-outputs 用于自动覆盖冲突文件
          echo "Running build_runner..."
          flutter pub run build_runner build --delete-conflicting-outputs
          
          # 编译 Web 版本
          # 使用 html 渲染器 (兼容性最强，防止 Canvaskit 在构建时内存溢出)
          # 增加 --verbose 以便如果再次失败，我们可以看到具体的报错代码行
          flutter build web --release --web-renderer html --verbose
          
          echo "Frontend build complete."

      # 5. 组装文件结构
      - name: Prepare Build Directory
        run: |
          mkdir -p deploy/dist
          
          # 移动编译好的前端文件
          mv AINoval/build/web deploy/dist/web
          
          # 兼容性复制
          cp -r deploy/dist/web deploy/dist/admin_web
          
          # 下载后端 JAR
          echo "Downloading Backend JAR..."
          wget -O deploy/dist/ainovel-server.jar https://github.com/Deng-m1/MaliangAINovalWriter/releases/download/v1.0.0/ainovel.jar
          
          # 验证目录
          ls -R deploy/dist/

      # 6. 登录 Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 7. 设置构建工具
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # 8. 构建并推送
      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: deploy/open/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ secrets.DOCKER_USERNAME }}/maliang-writer:latest
